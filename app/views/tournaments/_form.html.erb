<%= form_for(@tournament) do |f| %>
  <% if @tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

      <ul>
      <% @tournament.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :player_id, Player.all.map { |p| [p.name, p.id] } %>
  </div>
  <div class="field">
    <%= f.select :opening_id, Opening.all.map { |o| [o.name, o.id] } %>
  </div>
  <div class="field">
    <%= f.select :opponent_id, Player.all.map { |o| [o.name, o.id] } %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
